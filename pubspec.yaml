name: flutter_notebook_chapter_45
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.1

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter
  hugeicons: ^0.0.11
  percent_indicator: ^4.2.5
  smooth_page_indicator: ^1.2.1
  gap: ^3.0.1
  lorem_ipsum:

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
